derivean {
    http-server {
        port = 59080
        host = "http://localhost:59080"
    }
    pool {
        name = "derivean"
        url = "jdbc:h2:mem:derivean"
        user = "derivean"
        password = "derivean"
        size = 10
    }
}
