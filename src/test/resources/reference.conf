derivean {
    http-server {
        port = 99808
        host = "http://localhost:99808"
    }
    pool {
        name = "derivean"
        url = "jdbc:h2:mem:derivean"
        user = "derivean"
        password = "derivean"
        size = 10
    }
}
