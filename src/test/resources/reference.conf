derivean {
    http-server {
        port = 8080
        host = "http://localhost:8080"
    }
    pool {
        name = "derivean"
        url = "jdbc:h2:mem:derivean"
        user = "derivean"
        password = "derivean"
        size = 10
    }
}
