derivean {
    http-server {
        port = 8080
        host = "http://localhost:8080"
    }
    pool {
        name = "derivean"
        url = "jdbc:h2:./derivean.test"
        user = "derivean"
        size = 10
    }
}
